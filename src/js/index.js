(function handleLog() {
  console.log('this is index');
  const boo = Math.random() - .5;
  const foo = (x) => {
    console.log('this is ====>', x);
};
boo>0 && foo('Bio');

[1, 2, 3].map(n => n + 1);

})();
